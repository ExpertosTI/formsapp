node_modules
.git
legacy
.npm_cache
uploads

# Solo caché de Next — standalone y static SÍ van a la imagen
.next/cache
.next/trace

# Archivos grandes — volumen Docker, no imagen
public/uploads
migration_backup/data_migration_temp.json

*.tar.gz
.env
.env.*
